# =================================================================
# SZABLON KONFIGURACJI ŚRODOWISKOWEJ PTMS
# Skopiuj ten plik, zmień nazwę na .env i uzupełnij wartości.
# =================================================================

# --- KONFIGURACJA BAZY DANYCH (POSTGRES) ---
POSTGRES_DB=ptms_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword

# --- KONFIGURACJA BACKENDU (SPRING BOOT) ---
# Uwaga: w DB_URL używamy 'db', ponieważ tak nazywa się usługa w docker-compose
DB_URL=jdbc:postgresql://db:5432/ptms_db
DB_USERNAME=postgres
DB_PASSWORD=dbpassword
